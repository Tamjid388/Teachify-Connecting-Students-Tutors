model Category {
    id          String  @id @default(uuid())
    subject     String  @unique
    description String?
    thumbnail   String? 
    slug        String? @unique

    tutorCategory TutorCategory[]

    @@map("categories")
}
